<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/music_player_controls_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@android:color/transparent"
    android:orientation="vertical"
    android:paddingHorizontal="32dp"
    android:paddingBottom="8dp">

    <!-- Progress Bar Section -->
    <androidx.media3.ui.DefaultTimeBar
        android:id="@+id/exo_progress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:scrubber_color="@color/zetflix_accent"
        app:played_color="@color/zetflix_accent"
        app:unplayed_color="@color/player_progress_unplayed"
        app:buffered_color="@color/player_progress_buffered"
        app:bar_height="1.5dp"
        app:scrubber_enabled_size="8dp"
        app:scrubber_dragged_size="10dp" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp">

        <TextView
            android:id="@+id/exo_position"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/metropolis_medium"
            android:textColor="@color/player_text_secondary"
            android:textSize="11sp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/exo_duration"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/metropolis_medium"
            android:textColor="@color/player_text_secondary"
            android:textSize="11sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Main Controls Row: Shuffle | Prev | Play/Pause | Next | Timer -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp">

        <ImageButton
            android:id="@+id/music_player_shuffle"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/baseline_sync_24"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/player_text_primary"
            android:alpha="0.6"
            android:contentDescription="Shuffle" />

        <ImageButton
            android:id="@+id/exo_prev"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/baseline_skip_previous_24"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/exo_play_pause"
            app:layout_constraintStart_toEndOf="@+id/music_player_shuffle"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/player_text_primary"
            android:contentDescription="@string/previous" />

        <!-- Large White Play/Pause Button as per reference -->
        <ImageButton
            android:id="@+id/exo_play_pause"
            android:layout_width="72dp"
            android:layout_height="72dp"
            android:background="@drawable/bg_circle_white"
            android:src="@drawable/ic_baseline_play_arrow_24"
            android:padding="20dp"
            android:scaleType="fitCenter"
            app:tint="@color/black"
            android:contentDescription="Play or Pause"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/exo_next"
            app:layout_constraintStart_toEndOf="@+id/exo_prev"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
            android:id="@+id/exo_next"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_baseline_skip_next_24"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/music_player_repeat"
            app:layout_constraintStart_toEndOf="@+id/exo_play_pause"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/player_text_primary"
            android:contentDescription="@string/next" />

        <ImageButton
            android:id="@+id/music_player_repeat"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_baseline_clock_24"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/player_text_primary"
            android:alpha="0.6"
            android:contentDescription="Sleep Timer" />

    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Bottom Actions Row: Devices | Share | Queue -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp">

        <ImageButton
            android:id="@+id/music_player_devices"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_baseline_tv_24"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/player_text_primary"
            android:alpha="0.7"
            android:contentDescription="Devices" />

        <View
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@id/music_player_share"
            app:layout_constraintStart_toEndOf="@id/music_player_devices"
            app:layout_constraintTop_toTopOf="parent" />

        <ImageButton
            android:id="@+id/music_player_share"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_outline_share_24"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/music_player_download"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/player_text_primary"
            android:alpha="0.7"
            android:contentDescription="@string/result_share" />

        <ImageButton
            android:id="@+id/music_player_download"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/netflix_download"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toStartOf="@+id/music_player_queue"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/player_text_primary"
            android:alpha="0.7"
            android:contentDescription="@string/download" />

        <ImageButton
            android:id="@+id/music_player_queue"
            android:layout_width="40dp"
            android:layout_height="40dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:src="@drawable/ic_baseline_playlist_play_24"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:tint="@color/player_text_primary"
            android:alpha="0.7"
            android:contentDescription="@string/music_now_playing_queue" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</LinearLayout>
